# wdio-typescript-starter

WebdriverIO starter project with TypeScript, Mocha and Chai.

Contains one example test that fails on purpose.
Runs tests in Firefox and Chrome and reruns a failing spec file twice.
